/**
 * checks req.body.email param for validity
 * @param  {Request}  req 
 * @param  {Response} res 
 * @param {function} next
 * @returns {undefined} returns an http response containing a user's email and username
 */

const validateEmail = 